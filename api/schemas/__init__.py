"""Pydantic response/request models -- the HTTP contract.

Phase 0 ships only the shared primitives (``common.py``) and the health schema
(``health.py``). Per-view schemas (``backtest``, ``tearsheet``, ``etf``,
``macro``, ``volatility``, ``strategy``, ``jobs``) arrive with their phases.
"""
