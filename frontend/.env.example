# Frontend env (Vite). Copy to `.env` to override.
# Base URL of the FastAPI service. Leave unset in dev to use the Vite proxy
# (`/api` -> http://127.0.0.1:8000). Set to a full URL when not proxying.
# VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
