# Development / test dependencies. Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.0.3
pytest-cov
pre-commit
pyright

# --- API test deps (FastAPI TestClient uses httpx) — spec §8 ---
httpx==0.28.1
